{
  "name": "smart-text-rewriter",
  "version": "1.0.0",
  "description": "A Tampermonkey userscript that enhances all text fields with rewrite functionality using LLMs",
  "main": "src/smart-text-rewriter.user.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cat src/styles.css src/rewrite-modes.js src/smart-text-rewriter.user.js > dist/smart-text-rewriter.user.js",
    "start": "http-server . -o test-page.html"
  },
  "keywords": [
    "tampermonkey",
    "userscript",
    "text",
    "rewriter",
    "llm",
    "openai"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
